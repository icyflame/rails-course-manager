<%= form_for :departments, url: departments_path do |f| %>

<% if defined? @newdepartment %>

<% if @newdepartment.errors.any? %>

<div id="error_explanation">
	<h2><%= pluralize(@newdepartment.errors.count, "error") %> prohibited
		this department from being saved:
	</h2>
	<ul>
		<% @newdepartment.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
	</ul>
</div>

<% end %>
<% end %>

<p>
	<%= f.label :name_of_the_department %> <br/>
	<%= f.text_field :name, :class => "form-control"  %> 
</p>

<input type="submit" class="btn btn-md btn-success" value="Save department"/>

<br/>
<br/>

<a href="<%= departments_path %>">
	<button class="btn btn-md btn-success">
		department List
	</button>
</a>

<% end %>